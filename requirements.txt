# Ultra-simple - NO PANDAS (if all else fails)
# Use this if pandas keeps failing

# Core API
fastapi==0.109.0
uvicorn==0.27.0
pydantic==2.5.3
pydantic-settings==2.1.0

# HTTP
aiohttp==3.9.1

# Browser
playwright==1.41.0
beautifulsoup4==4.12.3

# Minimal data processing (no pandas)
python-dotenv==1.0.0
